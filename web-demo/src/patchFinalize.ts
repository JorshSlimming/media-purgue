// patchFinalize.ts — intentionally empty. The "Finalizar lote" button in the
// renderer App activates naturally when all files in a lote have been reviewed
// (pendingCount === 0). No DOM patching needed.
export {}
